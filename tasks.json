{
    "tasks": [
        {
            "label": "C++ compile",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "-std=c++14",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ]
        }
    ]
}

